# Documentação do Módulo de Conexão

## Descrição
este projeto implementa um padrão de conexão segura com o banco de dados,ultilizando **Design patterns** para evitar *hardcoding*

## o que foi feito 
- [x] criação do repositorio
- [x] implementação da conexão
- [x] resolução de conflito de merge
- [x] separação de cnfiguração

   ## Exemplo de uso
  Abaixo, o codigo padrão ulilizado pelo arquiteto:

      '''pyton
       # constante de configuração
      DB_HOST = *192.168..0.1*
       def conectar_banco():
          *** return f**conectdo ao {DB-HOST)*
